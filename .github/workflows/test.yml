name: test-actions

run-name: ${{ github.actor }} is testing out GitHub Actions

on:
  push:
    branches:
      - main
      - develop

jobs:
  actions-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Actions test
        run: ./install.sh
